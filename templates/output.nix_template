# Autogenerated by `bun2nix`, editing manually is not recommended
#
# Set of Bun packages to install
# Consume this with `pkgs.callPackage` providing
# the `root` arg
{ fetchurl, copyPathToStore, root, ... }:
{
  {%- for pkg in packages %}
  "{{ pkg.name }}" = {{ pkg.fetcher }};
  {%- endfor %}
}
